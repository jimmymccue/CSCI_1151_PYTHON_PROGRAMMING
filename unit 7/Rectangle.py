def calculate_area(width, length):
    """Calculate the area of a rectangle"""
    return width * length

def calculagte_perimeter(width, length):
    """Calculate the perimeter of a rectangle"""
    return width * 2 + length * 2